import MediaLibrary from '@/components/admin/MediaLibrary';

export const metadata = {
  title: 'Media Library | SafetyQuest Admin',
};

export default function MediaLibraryPage() {
  return <MediaLibrary />;
}