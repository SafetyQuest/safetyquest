{
  "name": "safetyquest",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @safetyquest/web dev",
    "build": "pnpm db:generate && pnpm --filter @safetyquest/web build",
    "build:azure": "./scripts/build-web.sh",
    "prepare:deploy": "./scripts/prepare-deploy.sh",
    "db:generate": "pnpm --filter @safetyquest/database db:generate",
    "db:push": "pnpm --filter @safetyquest/database db:push",
    "db:studio": "pnpm --filter @safetyquest/database db:studio"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}